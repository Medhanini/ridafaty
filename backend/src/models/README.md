# Models

With Prisma, models are defined in `prisma/schema.prisma`.
The generated TypeScript types live in `node_modules/@prisma/client`.

This folder exists as a convention placeholder. For complex domain objects
(value objects, computed properties, DTOs), add them here.
